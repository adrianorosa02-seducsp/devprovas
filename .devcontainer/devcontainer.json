{
  "name": "DevProvas Environment",
  "image": "://microsoft.com",
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "mhutchie.git-graph"
      ]
    }
  },
  "onCreateCommand": "sudo apt-get update && sudo apt-get install -y tmux && echo 'set -g mouse on' > ~/.tmux.conf"
}
